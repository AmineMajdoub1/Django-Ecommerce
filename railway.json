{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py collectstatic --noinput --clear && python manage.py migrate && gunicorn ecom.wsgi --log-file -"
  }
}
